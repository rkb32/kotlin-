name: Build Badge

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build Compilation Only
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: 21
        distribution: zulu
        cache: gradle

    - name: Build project (compilation only)
      run: ./gradlew assemble --no-daemon
